# Optional. Sans clé, l'app tourne en mode DEMO (catalogue local).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5

# SEO : URL canonique du site en production (sert canonical, sitemap, robots).
NEXT_PUBLIC_SITE_URL=https://cookaluna.fr

# Supabase (collecte emails, futurs comptes utilisateurs)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
